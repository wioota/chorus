<button class="dialog" data-dialog="InstancesNew">{{t "instances.add.title"}}</button>
{{> multipleSelectionHeader}}
{{#if loaded}}
    <div class="count">
        <span class="number">{{count}}</span> {{pluralize count "instances.title"}}
    </div>
{{else}}
    <div class="loading">{{t "loading"}}</div>
{{/if}}
