<div class='instance_provider gpdb_instance'>
    <div class="details">{{t "instances.list.type.data_sources"}}</div>
    <ul class="list selectable">
        {{#each greenplum}}
            <li class="instance" data-type="{{entityType}}" data-instance-id="{{id}}">{{renderTemplate "instance" this}}</li>
        {{/each}}
    </ul>
    {{#unless hasGreenplum}}
        <div class="no_instances">{{t "instances.none"}}</div>
    {{/unless}}
</div>

<div class="instance_provider hadoop_instance">
    <div class="details">{{t "instances.list.type.hdfs"}}</div>
    <ul class="list selectable">
        {{#each hadoop}}
            <li class="instance" data-type="{{entityType}}" data-instance-id="{{id}}">{{renderTemplate "hadoop_instance" this}}</li>
        {{/each}}
    </ul>
    {{#unless hasHadoop}}
        <div class="no_instances">{{t "instances.none"}}</div>
    {{/unless}}
</div>

<div class="instance_provider gnip_instance">
    <div class="details">{{t "instances.list.type.gnip"}}</div>
    <ul class="list selectable">
        {{#each gnip}}
        <li class="instance" data-type="{{entityType}}" data-instance-id="{{id}}">{{renderTemplate "instance" this}}</li>
        {{/each}}
    </ul>
    {{#unless hasGnip}}
    <div class="no_instances">{{t "instances.none"}}</div>
    {{/unless}}
</div>