{{#if models}}
   {{#each models}}
      <li data-id="{{id}}" class="tag_list_item">
          <div>{{this.name}}</div>
       </li>
   {{/each}}
{{else}}
    <div class="no_tags">{{t "tags.none"}}</div>
{{/if}}